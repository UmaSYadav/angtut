import { IhdeshowDirective } from './ihdeshow.directive';

describe('IhdeshowDirective', () => {
  it('should create an instance', () => {
    const directive = new IhdeshowDirective();
    expect(directive).toBeTruthy();
  });
});
